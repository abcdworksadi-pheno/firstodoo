# Docker Compose configuration for Odoo CI/CD testing
# Used by GitHub Actions workflow to run module tests

services:
  db:
    image: postgres:15
    container_name: odoo-test-db
    environment:
      POSTGRES_DB: odoo
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
    volumes:
      - test-db-data:/var/lib/postgresql/data
    networks:
      - test-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo"]
      interval: 5s
      timeout: 5s
      retries: 5

  odoo:
    image: odoo:18.0
    container_name: odoo-test
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8069:8069"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: odoo
      DB_PASSWORD: odoo
      DB_NAME: odoo
    volumes:
      - odoo-test-data:/var/lib/odoo
      - ./custom_addons:/mnt/extra-addons
    networks:
      - test-network
    command: >
      odoo
      --db_host=db
      --db_port=5432
      --db_user=odoo
      --db_password=odoo
      --database=odoo
      --addons-path=/mnt/extra-addons
      --init=task_planning,employee_mood,sale_stock_check
      --stop-after-init

volumes:
  test-db-data:
  odoo-test-data:

networks:
  test-network:
    driver: bridge
